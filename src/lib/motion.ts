// Shared entrance-animation values so hero and navbar stay one choreography.
export const EASE: [number, number, number, number] = [0.25, 1, 0.5, 1];

// Load choreography order: hero eyebrow -> headline -> subtitle -> buttons
// (0.08 stagger), carousel at 0.4, navbar logo lands last.
export const CAROUSEL_DELAY = 0.4;
export const LOGO_DELAY = 0.55;
